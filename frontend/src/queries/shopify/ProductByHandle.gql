query ProductByHandle($handle: String!) {
	productByHandle(handle: $handle) {
		title
		description
		descriptionHtml
		featuredImage {
			altText
			height
			url
			width
		}
		options {
			name
			values
		}
		variants(first: 10) {
			edges {
				node {
					sku
					selectedOptions {
						name
						value
					}
					media(first: 10) {
						edges {
							node {
								alt
								status
								... on MediaImage {
									preview {
										image {
											url
											width
											height
										}
									}
								}
							}
						}
					}
				}
			}
		}
	}
}
