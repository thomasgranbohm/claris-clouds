query ProductByHandle($handle: String!) {
	product(handle: $handle) {
		title
		descriptionHtml
		featuredImage {
			altText
			height
			url
			width
		}
		options {
			name
			values
		}
		variants(first: 10) {
			edges {
				node {
					sku
					id
					currentlyNotInStock
					selectedOptions {
						name
						value
					}
					price {
						amount
						currencyCode
					}
					image {
						height
						url
						width
					}
				}
			}
		}
	}
}
