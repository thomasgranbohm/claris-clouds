query Artworks($start: Int = 0) {
	artworks(sort: "id:desc", pagination: { start: $start, limit: 9 }) {
		data {
			attributes {
				name
				image {
					data {
						attributes {
							...UploadFile
						}
					}
				}
				slug
			}
		}
		meta {
			pagination {
				total
				page
				pageSize
				pageCount
			}
		}
	}
}

fragment UploadFile on UploadFile {
	name
	alternativeText
	caption
	width
	height
	hash
	ext
	formats
}
